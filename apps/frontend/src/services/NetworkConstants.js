export const NETWORK_CONFIG = {
  UPDATE_RATE: 50, // 20 updates per second
  INTERPOLATION_DELAY: 100, // 100ms interpolation delay
  SNAPSHOT_RATE: 3000, // Full state sync every 3 seconds
  CORRECTION_THRESHOLD: 5, // Position difference threshold for corrections
};
